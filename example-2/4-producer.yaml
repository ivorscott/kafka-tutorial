apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-kafka-producer
  namespace: kafka
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-kafka-producer
  template:
    metadata:
      labels:
        app: go-kafka-producer
    spec:
      containers:
        - name: producer
          image: kafka-producer-go:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: kafka-cluster-kafka-bootstrap.kafka:9092
            - name: TOPIC
              value: my-topic
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
